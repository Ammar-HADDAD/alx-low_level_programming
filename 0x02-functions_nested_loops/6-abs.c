#include "main.h"

/**
 * _abs - Computing the absolute value of an number.
 * @n: The input integer.
 *
 * Return: The absolute value of @n.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
